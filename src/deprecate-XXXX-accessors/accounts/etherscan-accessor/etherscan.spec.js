'use strict';
/*
const chai = require('chai');
const expect = chai.expect;

const etherscan = require('./etherscan');
const addresses = require('../../../app/config').addresses;
const currencies = require('../../currencies');

describe('etherscan', () => {
  let addr;

  xit ('gets latest Ether balance for a single address', async () => {
    const balance = await etherscan.getLatestEthBalance(addresses[0]);
    expect(typeof balance).to.be.equal('number');
    expect(balance).to.be.gte(0.0);
  });

  xit ('gets External records for a single address', async () => {
    const recs = await etherscan.getExtRecords(addresses[6]);
    expect(recs.length).to.be.gt(0);
  });

  xit ('gets Internal records for a single address', async () => {
    const recs = await etherscan.getIntRecords(addresses[6]);
    expect(recs.length).to.be.gt(0);
  });

  xit ('gets Ethereum records for a single address', async () => {
    const recs = await etherscan.getEthRecords(addresses[0]);
    expect(recs.length).to.be.gt(0);
  });

  it ('gets Token records for a single address', async () => {
    const recs = await etherscan.getTokRecords('HBT', addresses[0]);
    expect(recs.length).to.be.gt(0);
  });
});
*/