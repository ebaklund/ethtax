'use strict';

const chai = require('chai');

const expect = chai.expect;
const given = describe;
const when = describe;

describe ('report-formatter', () => {
  given ('a OutputTransactionTable', () => {
    when ('formatted', () => {
      it ('aligns fields in columns', () => {
        expect(false).to.be.true;
      });
    });
  });
});